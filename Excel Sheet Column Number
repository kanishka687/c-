class Solution {
public:
    int titleToNumber(string columnTitle) {
        long long res = 0; // Use long long to prevent potential overflow during calculation
        for (char c : columnTitle) {
            int digit = c - 'A' + 1;
            res = res * 26 + digit;
        }
        return (int)res;
    }
};
