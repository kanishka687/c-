class Solution {
public:
    int hammingWeight(int n) {
        int count = 0;
        while (n > 0) {
            // Brian Kernighan's trick: n & (n - 1) removes the rightmost 1
            n &= (n - 1);
            count++;
        }
        return count;
    }
};
